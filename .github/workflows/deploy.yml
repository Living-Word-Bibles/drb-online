name: Build & Deploy DRB to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: --max-old-space-size=6144
    steps:
      - name: Checkout (with Git LFS)
        uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Quick repo scan (non-fatal)
        shell: bash
        run: |
          set -e
          echo "Root listing:"
          ls -la
          echo "If present, show first lines & size of EntireBible-DR.json:"
          [ -f EntireBible-DR.json ] && head -n 3 EntireBible-DR.json || true
          [ -f EntireBible-DR.json ] && wc -c EntireBible-DR.json || true
          echo "Sample files under Douay-Rheims/ (if exists):"
          [ -d Douay-Rheims ] && find Douay-Rheims -type f | head -n 30 || true
          echo "Sample files under Bible-DouayRheims-main/ (if exists):"
          [ -d Bible-DouayRheims-main ] && find Bible-DouayRheims-main -type f | head -n 30 || true

      - name: Build site
        run: node build.mjs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
