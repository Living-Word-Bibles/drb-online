name: Build & Deploy DRB to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Debug tree (top 2 levels)
        run: |
          node -v
          ls -la
          echo "---- data (recursive 2) ----"
          find data -maxdepth 2 -type f -printf "%p\n" 2>/dev/null || true
          echo "---- Bible-DouayRheims-main (recursive 2) ----"
          find Bible-DouayRheims-main -maxdepth 2 -type f -printf "%p\n" 2>/dev/null || true

      - name: Build DRB
        run: node build.mjs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
